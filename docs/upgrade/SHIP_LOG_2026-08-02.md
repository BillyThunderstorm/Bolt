# Ship log — 2026-08-02

## Grok LLM layer + natural conversation (PR #3)

### Shipped

1. **`Core/modules/LLM_Handler.py`**
   - Multi-provider: `openai` | `xai`
   - Env: `BOLT_LLM_PROVIDER`, `BOLT_LLM_FALLBACK`, `XAI_API_KEY`, model overrides
   - Shared `ask_llm(prompt, system=, history=, …)` for the whole project

2. **`Core/Bolt_Conversation.py`**
   - Replies via `ask_llm` (Grok when provider=xai)
   - Calls `Intent_Router` before free-form chat

3. **`Core/modules/Bolt_Chat.py`**
   - `!Bolt` answers via the same `ask_llm` path

4. **`Core/modules/Intent_Router.py`** (new)
   - Plain language → real actions:
     - morning briefing
     - next actions
     - manager status
     - queue status
     - research / mission status

5. **`.env.example`**
   - Documents LLM provider vars

### Operator setup

```bash
# .env
BOLT_LLM_PROVIDER=xai
BOLT_LLM_FALLBACK=none
XAI_API_KEY=...

git pull origin main
PYTHONPATH=Core python3 -m modules.LLM_Handler
PYTHONPATH=Core python3 -m Bolt_Conversation --text
```

### Docs updated same night

- `Core/modules/BOLT_COMMANDS.md`
- `docs/upgrade/UPGRADE_STATUS.md`
- `docs/upgrade/NEXT_UPGRADE_STEPS.md`
- `docs/INDEX.md`
- this ship log

### Not engineering (still open for Billy)

- C5 research decisions
- Real product ASINs + first shipped posts
- TikTok OAuth when ready to auto-publish
