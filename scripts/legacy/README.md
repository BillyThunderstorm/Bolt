# Legacy Scripts

This folder keeps older root-level scripts that were replaced by cleaner entrypoints.

- Use `scripts/get_twitch_token.py` instead of the old OAuth localhost script.
- Use `modules/Brain_Controller.py` instead of the legacy root `Brain_Controller.py`.
- Use `modules/Twitch_API.py` instead of the legacy root `Twitch_API.py`.

These files are retained for reference/history and should not be used for new workflows.
