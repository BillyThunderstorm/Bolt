
# ── Archive / backups ─────────────────────────────────────────────────────────
# ── Editor / IDE ──────────────────────────────────────────────────────────────
# ── Mac system files ──────────────────────────────────────────────────────────
# ── Python virtual environment ────────────────────────────────────────────────
# ── Runtime output — generated automatically, no need to track ───────────────
# ── Secrets — NEVER commit these ─────────────────────────────────────────────
# ── Video files — too large for GitHub ───────────────────────────────────────
*.avi
*.mkv
*.mov
*.mp4
*.pyc
*.pyo
*.swp
.AppleDouble
.DS_Store
.env
.env.backup
.idea/
.venv*/
.venv-1/
.venv/
.vscode/
_Archive/
__pycache__/
archive/
archive/docs.env
clips/
credentials.json
Data/archive/
Data/data/
Data/logs/
docs.env
env/
llm-env/
logs/
node_modules/
recordings/
scripts/launcher_stderr.log
scripts/launcher_stdout.log
venv/
vertical_clips/
yolov5
.env.local
.env.local.bak
configs/storage_alerts.env
configs/*.secrets.env

# ── Third-party SDK checkouts, build outputs, and bundled apps — never tracked ─
google-cloud-sdk/
dist/
build/
llm/
3rd_Party/google-cloud-sdk/
3rd_Party/llm/
*.app/

# Twitch token cache (contains live access_token; regenerated by get_twitch_token.py)
Core/data/twitch_token_cache.json
